create or replace trigger stat_insert
before delete or insert on library_audit
begin 
if rtrim (upper(to_char(sysdate,'day')))='WEDNESDAY' then 
raise_application_error ( -200000,'cant delete');
end if;
end;
/
