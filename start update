create or replace trigger Audit_update
before delete or update on Audit_t
begin 
if rtrim (upper(to_char(sysdate,'month')))='JULY' then 
raise_application_error ( -200000,'cant delete');
end if;
end;
/
